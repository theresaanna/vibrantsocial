services:
  db:
    image: postgres:16-alpine
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: vibrantsocial
      POSTGRES_PASSWORD: vibrantsocial
      POSTGRES_DB: vibrantsocial
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
