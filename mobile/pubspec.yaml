name: vibrantsocial
description: "VibrantSocial mobile app."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # HTTP client for talking to the VibrantSocial API.
  dio: ^5.9.0

  # Reactive state management.
  flutter_riverpod: ^2.6.1

  # Keychain-backed JWT storage (EncryptedSharedPreferences on Android).
  flutter_secure_storage: ^9.2.4

  # Native OAuth — we exchange the provider-issued ID token for a mobile
  # JWT at /api/v1/auth/oauth/native.
  google_sign_in: ^6.2.2
  sign_in_with_apple: ^6.1.4

  # Open external URLs (bio links, profile links) in the platform browser.
  url_launcher: ^6.3.2

  # Network image widget with disk caching + broader format support
  # (handles AVIF / HEIC / animated WebP the stock Android decoder can't).
  cached_network_image: ^3.4.1

  # System photo picker for composer attachments.
  image_picker: ^1.1.2

  # Lexend is the app-wide base font; google_fonts also lazy-loads the
  # per-user username fonts selected via theme settings on the web.
  google_fonts: ^6.2.1

  # Realtime transport for DMs and chatrooms. Token is issued by
  # GET /api/ably-token; channels follow the server's naming convention
  # (chat:{conversationId}, chatroom:{slug}).
  ably_flutter: ^1.2.39

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Bundled display fonts offered to premium users as a username font on
  # the web app. Keep in sync with `mobile/scripts/download-username-fonts.sh`
  # and `src/lib/profile-fonts.ts`. Family names match how the web Google
  # Fonts loader references them.
  fonts:
    - family: Sofadi One
      fonts: [{ asset: assets/fonts/SofadiOne.ttf }]
    - family: Jersey 10
      fonts: [{ asset: assets/fonts/Jersey10.ttf }]
    - family: Limelight
      fonts: [{ asset: assets/fonts/Limelight.ttf }]
    - family: Unkempt
      fonts: [{ asset: assets/fonts/Unkempt.ttf }]
    - family: Gugi
      fonts: [{ asset: assets/fonts/Gugi.ttf }]
    - family: Turret Road
      fonts: [{ asset: assets/fonts/TurretRoad.ttf }]
    - family: Nova Mono
      fonts: [{ asset: assets/fonts/NovaMono.ttf }]
    - family: Ewert
      fonts: [{ asset: assets/fonts/Ewert.ttf }]
    - family: Ballet
      fonts: [{ asset: assets/fonts/Ballet.ttf }]
    - family: Manufacturing Consent
      fonts: [{ asset: assets/fonts/ManufacturingConsent.ttf }]
    - family: Rubik Puddles
      fonts: [{ asset: assets/fonts/RubikPuddles.ttf }]
    - family: Hachi Maru Pop
      fonts: [{ asset: assets/fonts/HachiMaruPop.ttf }]
    - family: Ms Madi
      fonts: [{ asset: assets/fonts/MsMadi.ttf }]
    - family: Jacquard 24
      fonts: [{ asset: assets/fonts/Jacquard24.ttf }]
    - family: Texturina
      fonts: [{ asset: assets/fonts/Texturina.ttf }]
    - family: Great Vibes
      fonts: [{ asset: assets/fonts/GreatVibes.ttf }]
    - family: Rye
      fonts: [{ asset: assets/fonts/Rye.ttf }]
    - family: Bonbon
      fonts: [{ asset: assets/fonts/Bonbon.ttf }]
    - family: Agu Display
      fonts: [{ asset: assets/fonts/AguDisplay.ttf }]
    - family: Agbalumo
      fonts: [{ asset: assets/fonts/Agbalumo.ttf }]

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
